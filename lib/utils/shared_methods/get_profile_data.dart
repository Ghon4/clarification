//  for getting profile data from api
// getProfileData() {
//   ProfileRepositoryImpl(ProfileDataSourceImpl()).getProfile().then((value) {
//     if (value.isRight()) {
//       AppConstants.shared.setUser(value.getOrElse(() => ProfileEntity()));
//     }
//   });
// }
